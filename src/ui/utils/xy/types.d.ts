import { StepCtrl, THREE_NUMBERS } from '../../../utils/numbers';

export type XYStepSettings = {
  x: THREE_NUMBERS;
  y: THREE_NUMBERS;
};

export type XYStepCtrls = {
  x: StepCtrl;
  y: StepCtrl;
};
